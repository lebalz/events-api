{  
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "skipLibCheck": true, // better-auth failures for sqlite, timer and zod, try turning on later...
  },
  "exclude": ["**/*.test.*", "**/__mocks__/*", "**/__tests__/*"]
}